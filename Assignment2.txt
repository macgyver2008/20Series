Aniaml.java --
public class Animal{
    /**
    * Color of animal.
    */
    private static String color = "";
    /**
    * Name of animal.
    */
    private static String name = "";
    /**
    * limited speed of animal.
    */
    private static int maxSpeed = 0;
    /**
    * Speed of animal.
    */
    private static int speed = 0;

    /**
     * @param fColor
     * @param fLicensePlate
     * @param fName
     * @param fMaxSpeed
     */
    public Animal(final String fColor, final String fName,
    final int fMaxSpeed) {
        this.color = fColor;
        this.name = fName;
        this.maxSpeed = fMaxSpeed;
        this.speed = 0;
    }

    /**
     * @param power
     * @param time
     * @return speed
     * which is animal's speed.
     */
    public int accelerate(final int power, final int time) {
        /*
        * Accelerate, speed = power * time
        * speed <= maxSpeed
        */
        speed += power * time;
        if (speed <= maxSpeed) {
            return speed;
        } else {
            speed = maxSpeed;
            return speed;
        }
    }

    /**
     * @param power
     * @param time
     * @return speed
     * which is car's speed.
     */
    public int brake(final int power, final int time) {
        // break, speed = speed - power * time
        // speed >= 0
       speed -= power * time;
        if (speed >= 0) {
            return speed;
        } else {
            speed = 0;
            return speed;
        }
    }


}



Hare.java-- 

public class Hare extends Animal{

        /**
    * Color of animal.
    */
    private String color = "White";
    /**
    * Name of animal.
    */
    private String name = "Hare";
    /**
    * limited speed of animal.
    */
    private int maxSpeed = 20;
    /**
    * Speed of animal.
    */
    private int speed = 0;
    /**
     * distance to travel
     */
    private int distance = 0;

    /**
     * @param fColor
     * @param fName
     * @param fMaxSpeed
     */
    public Hare() {
        super("White", "Hare", 20);
    }

    public void setdistance(int distanceSetter) {
        distance = distanceSetter;
    }

    public void run(int speed){
        distance -= speed;
    }

    /**
     * @param power
     * @param time
     * @return speed
     */
    @Override
    public int accelerate(final int power, final int time) {
        /**
        * Accelerate, speed = power * time
        * speed <= maxSpeed
        */
        speed += power * time;
        if (speed <= maxSpeed) {
            return speed;
        } else {
            speed = maxSpeed;
            return speed;
        }
    }

    /**
     * @param power
     * @param time
     * @return speed
     * which is car's speed.
     */
    @Override
    public int brake(final int power, final int time) {
        // break, speed = speed - power * time
        // speed >= 0
       speed -= power * time;
        if (speed >= 0) {
            return speed;
        } else {
            speed = 0;
            return speed;
        }
    }
    /**
     * @param chance
     * @return sleep
     */
    public int specialSkill(int chance) {
        if (chance >= 70) {
            brake(100, 5);
            System.out.println("Special (dis)avility! take a nap zzz");
            System.out.println(name + " is fell a sleep for 10 turn");
            return 10;
        }
        return 0;
    }

    public int getSpeed() {
        // System.out.println(name + "'s speed is now " + speed + "km/h");
        return speed;
    }

    public void introduce() {
        System.out.println("Name: " + name);
        System.out.println("Max speed:" + maxSpeed);
        System.out.println("her message: I'm 80 years old... tired");
    }
}

Tortoise.java --
public class Tortoise extends Animal{

        /**
    * Color of animal.
    */
    private String color = "Green";
    /**
    * Name of animal.
    */
    final private String name = "Tortoise";
    /**
    * limited speed of animal.
    */
    final private int maxSpeed = 3;
    /**
    * Speed of animal.
    */
    private int speed = 0;
    /**
     * distance to travel
     */
    private int distance = 1000;
    /**
     * @param fColor
     * @param fName
     * @param fMaxSpeed
     */
    public Tortoise() {
        super("Green", "Tortoise", 3);
    }

    public void setdistance(int distanceSetter) {
        distance = distanceSetter;
    }

    public void run(int speed){
        distance -= speed;
    }
    /**
     * @param power
     * @param time
     * @return speed
     */
    @Override
    public int accelerate(final int power, final int time) {
        /**
         * speed (0 - 100)
         * time (1 - 5)
        * Accelerate, speed = power * time
        * speed <= maxSpeed
        */
        speed += (power/10) * time;
        if (speed <= maxSpeed) {
            run(speed);
            return speed;
        } else {
            speed = maxSpeed;
            run(speed);
            return speed;
        }
    }

    /**
     * @param power
     * @param time
     * @return speed
     * which is car's speed.
     */
    @Override
    public int brake(final int power, final int time) {
        // break, speed = speed - power * time
        // speed >= 0
       speed -= power * time;
        if (speed >= 0) {
            return speed;
        } else {
            speed = 0;
            return speed;
        }
    }
    /**
     * @param chance
     * @return sleep
     */
    public int specialSkill(int chance) {
        if (chance >= 80) {
            brake(100, 5);
            System.out.println("Special (dis)avility! he is tired..");
            System.out.println(name + " will break for 2 turn");
            if (chance >= 90) {
                System.out.println("OMG!! he rolls");
                distance -= 100;
            }
            return 2;
        }
        return 0;
    }

    public int getSpeed() {
        // System.out.println(name + "'s speed is now " + speed + "km/h");
        return speed;
    }
    public void introduce() {
        System.out.println("Name: " + name);
        System.out.println("Max speed:" + maxSpeed);
        System.out.println("his message: I'm fast");
    }
}

Tung Sahur.java
public class Tungtungtungtungtungtungtungtungtungsahur extends Animal{

    /**
    * Color of animal.
    */
    private String color = "Wooden Brown";
    /**
    * Name of animal.
    */
    final private String name = "Tungtungtungtungtungtungtungtungtungsahur";
    /**
    * limited speed of animal.
    */
    final private int maxSpeed = 99999999;
    /**
    * Speed of animal.
    */
    private int speed = 0;
    /**
     * distance to travel
     */
    private int distance = 1000;
    /**
     * @param fColor
     * @param fName
     * @param fMaxSpeed
     */
    public Tungtungtungtungtungtungtungtungtungsahur() {
        super
        ("Wooden Brown", "Tungtungtungtungtungtungtungtungtungsahur", 99999999);
    }

    public void setdistance(int distanceSetter) {
        distance = distanceSetter;
    }

    public void run(int speed){
        distance -= speed;
    }
    /**
     * @param power
     * @param time
     * @return speed
     */
    @Override
    public int accelerate(final int power, final int time) {
        System.out.println("Sahur time is yet...");
        return 0;
    }

    /**
     * @param power
     * @param time
     * @return speed
     * which is car's speed.
     */
    @Override
    public int brake(final int power, final int time) {
        return 0;
    }
    /**
     * @param chance
     * @param tortoiseDistance
     * @param hareDistance
     * @return sahurTime
     */
    public int specialSkill(int chance) {
        if (chance >= 90) {
            System.out.println
            ("TUNG TUNG TUNG TUNG TUNG TUNG TUNG TUNG TUNG SAHUR");
            return 3;
        }
        return 0;
    }

    public int avtivateSkill(int chance, int tortoiseDistance, int hareDistance) {
            if (chance <= 50) {
                distance = tortoiseDistance + 75;
            } else {
                distance = hareDistance + 75;
            }
            return distance;
    }
    public int getSpeed() {
        // System.out.println(name + "'s speed is now " + speed + "km/h");
        return speed;
    }
    public void introduce() {
        System.out.println("Name: " + name);
        System.out.print("Tung, tung, tung, tung, tung, tung, tung, tung,");
        System.out.print("tung Sahur. Anomali mengerikan yang hanya keluar");
        System.out.print(" pada sahur.\n Konon katanya kalau ada orang yang");
        System.out.print(" dipanggil sahur tiga kali dan tidak nyaut ....");
        System.out.print("Tung, tung, tung, tung, tung, tung, tung! Share ");
        System.out.println("ke teman kalian yang susah sahur.");
    }
}


Helloowfs v.java
import java.util.Random;

/*
 * The "Hare and tortoise" program, just proves you can print to terminal.
 *
 * @author  Yoochan Han
 * @version 1.0
 * @since   2025-12-01
 */

/**
* This is the standard "Hare and tortoise" program.
 */
public final class UselessAnimalRacing {

    /**
     * Prevent instantiation.
     */
    private UselessAnimalRacing() {
        throw new IllegalStateException("Cannot be instantiated");
    }

    /**
     * The starting main() function.
     *
     * @param args No args will be used
     */
    public static void main(final String[] args) {

        // Create animals
        Hare hare = new Hare();
        Tortoise tortoise = new Tortoise();
        Tungtungtungtungtungtungtungtungtungsahur sahur = 
        new Tungtungtungtungtungtungtungtungtungsahur();
        Random random = new Random();

        final int track = 1000;   // finish distance
        int hareDistance = track;
        int tortoiseDistance = track;
        int sahurDistance = track;

        int hareSleep = 0;        // hare sleeping turns
        int turtleSleep = 0;      // tortoise resting turns
        int sahurTime = 0;        // Sahut timer

        int turn = 1;
        
        tortoise.setdistance(track);
        hare.setdistance(track);
        sahur.setdistance(track);
        System.out.println("=============================================");
        System.out.println("      Welcome to Silver Forest Racing!       ");
        System.out.println("=============================================");
        delay(2);

        hare.introduce();
        delay(2);
        tortoise.introduce();
        delay(2);
        sahur.introduce();

        System.out.println("\nRace Start!\n");

        // GAME LOOP
        while (hareDistance > 0 &&
        tortoiseDistance > 0 &&
        sahurDistance > 0) {

            System.out.println("---------- TURN " + turn + " ----------");

            // 1) HARE TURN -------------------------------------------------
            if (hareSleep > 0) {
                System.out.println("Hare is sleeping... remaining turns: " +
                hareSleep);
                hareSleep--;
            } else {
                int power = random.nextInt(30) + 1; // 1~30
                int time = random.nextInt(3) + 1;   // 1~3

                hare.accelerate(power, time);
                hareDistance -= hare.getSpeed();

                // special skill chance
                int chance = random.nextInt(100) + 1;
                hareSleep = hare.specialSkill(chance);

                System.out.println("Hare speed: " + hare.getSpeed());
                System.out.println("Hare remaining distance: " +
                Math.max(hareDistance, 0));
            }

            // 2) TORTOISE TURN ----------------------------------------------
            if (turtleSleep > 0) {
                System.out.println("Tortoise is resting... remaining turns: " +
                turtleSleep);
                turtleSleep--;
            } else {
                int power = random.nextInt(30) + 1; // 1~30
                int time = random.nextInt(3) + 1;   // 1~3

                tortoise.accelerate(power, time);
                tortoiseDistance -= tortoise.getSpeed();

                // special skill chance
                int chance = random.nextInt(100) + 1;
                turtleSleep = tortoise.specialSkill(chance);

                System.out.println("Sahur is... stop??");
                System.out.println("Tortoise remaining distance: " +
                Math.max(tortoiseDistance, 0));
            }

            // 3) SAHUR TURN ----------------------------------------------
            if (sahurTime > 0) {
                System.out.println("Tung Tung.. Sahur: Sahur!");
                sahurTime--;
            } else if (sahurTime == 1) {
                int chance = random.nextInt(100) + 1;
                sahurTime = sahur.specialSkill(chance);
                sahurDistance = 
                sahur.avtivateSkill(chance, tortoiseDistance, hareDistance);
                System.out.println
                ("TUNG TUNG TUNG TUNG TUNG TUNG TUNG TUNG TUNG SAHUR");
            }else {
                int power = random.nextInt(30) + 1; // 1~30
                int time = random.nextInt(3) + 1;   // 1~3

                sahur.accelerate(power, time);
                sahurDistance -= sahur.getSpeed();

                // special skill chance
                int chance = random.nextInt(100) + 1;
                sahurTime = sahur.specialSkill(chance);

                System.out.println("Sahur's speed:.. Stop? ");
                System.out.println("T.Sahur remaining distance: " +
                Math.max(sahurDistance, 0));
            }

            // WIN CONDITION ---------------------------------------------------
            if (hareDistance <= 0 && tortoiseDistance <= 0
            && sahurDistance <= 0) {
                System.out.println("\nIt's a TIE!!!");
                break;
            } else if (hareDistance <= 0) {
                System.out.println("\nHare wins the race!");
                break;
            } else if (tortoiseDistance <= 0) {
                System.out.println("\nTortoise wins the race!");
                break;
            } else if (sahurDistance <= 0) {
                System.out.println
                ("\nTungtungtungtungtungtungtungtungtungsahur wins the race!");
                break;
            }

            turn++;
            // delay(1);
            System.out.println();
        }

        System.out.println("\n===== RACE FINISHED =====");
    }

    /**
     * Delay method (seconds)
     */
    public static void delay(final int time) {
        try {
            Thread.sleep(time * 1000);
        } catch (InterruptedException e) {
            e.printStackTrace();
        }
    }
}
